spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gym
    username: chan
    password: 123
#    driverClassName: jdbc:mysql://localhost:3306/gym
    hikari:
      maximum-pool-size: 20

  batch:
    jdbc:
      initialize-schema: always


---

spring:
  config.activate.on-profile: test
  datasource.url: jdbc:h2:mem:testdb

